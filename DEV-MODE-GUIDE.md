# 开发模式使用指南

## 🎯 什么是开发模式？

开发模式允许你在**没有配置 Supabase** 的情况下，直接预览 Studio 功能。系统会自动使用模拟数据，让你可以：

- ✅ 直接访问 Studio 页面，无需登录
- ✅ 查看项目列表（示例项目）
- ✅ 创建新项目（模拟数据）
- ✅ 进入工作区，预览所有阶段功能
- ✅ 测试 UI 和交互流程

## 🚀 如何使用

### 自动启用

开发模式会在以下条件下**自动启用**：

1. ✅ 运行在开发环境（`NODE_ENV === 'development'`）
2. ✅ **且** Supabase 未配置（`.env.local` 文件不存在或没有 Supabase 配置）

**无需任何额外配置！**

### 访问 Studio

1. 启动开发服务器：
   ```bash
   npm run dev
   ```

2. 直接访问 Studio：
   ```
   http://localhost:3000/studio
   ```

3. **无需登录**，直接进入！

## 📋 开发模式功能

### ✅ 可用功能

- **项目列表**：显示 2 个示例项目
- **创建项目**：可以创建新项目（保存在内存中）
- **工作区**：可以进入项目工作区
- **所有阶段**：Scope、Stack、Design、Build 都可以预览
- **UI 交互**：所有按钮、表单、导航都可以测试

### ⚠️ 限制

- **数据不持久化**：刷新页面后，创建的项目会消失
- **AI 功能不工作**：需要配置 `GEMINI_API_KEY` 才能使用 AI 功能
- **数据不保存**：所有操作都是模拟的，不会真正保存到数据库

## 🔧 开发模式标识

启用开发模式后，页面顶部会显示黄色横幅：

```
🔧 开发模式 | 已启用 - 使用模拟数据预览功能 | 用户: dev@byvibe.ai
```

## 📝 关于部署

### 部署到服务器

**是的，部署到服务器后就没有这些问题了！**

在 Cloudflare Pages（或其他平台）部署时：

1. ✅ **环境变量已配置**：在 Cloudflare Dashboard 中配置了 Supabase 环境变量
2. ✅ **自动加载**：环境变量会在构建时自动注入
3. ✅ **无需 `.env.local`**：生产环境使用平台的环境变量配置

### 本地开发 vs 生产环境

| 功能 | 本地开发（无配置） | 本地开发（有配置） | 生产环境 |
|------|------------------|------------------|---------|
| 开发模式 | ✅ 自动启用 | ❌ 不启用 | ❌ 不启用 |
| 注册/登录 | ❌ 需要配置 | ✅ 可用 | ✅ 可用 |
| 数据持久化 | ❌ 模拟数据 | ✅ 真实数据 | ✅ 真实数据 |
| AI 功能 | ❌ 需要配置 | ✅ 可用 | ✅ 可用 |

## 🎯 使用场景

### 场景 1: 快速预览 UI

**目标**：只想看看 Studio 的界面和交互

**方法**：
- 直接运行 `npm run dev`
- 访问 `/studio`
- 无需任何配置，直接预览

### 场景 2: 完整功能测试

**目标**：测试完整的注册、登录、数据保存功能

**方法**：
1. 创建 `.env.local` 文件
2. 配置 Supabase 环境变量
3. 重启服务器
4. 现在可以使用完整功能

### 场景 3: 部署到生产

**目标**：部署到 Cloudflare Pages

**方法**：
1. 在 Cloudflare Dashboard 配置环境变量
2. 推送代码
3. 自动部署，功能完整可用

## 💡 提示

- **开发模式只在本地开发环境生效**
- **生产环境永远不会启用开发模式**
- **开发模式只是为了方便预览，不影响生产环境**

## 🔄 切换模式

### 从开发模式切换到完整功能

1. 创建 `.env.local` 文件
2. 添加 Supabase 配置
3. 重启服务器
4. 开发模式自动关闭，使用真实功能

### 从完整功能切换回开发模式

1. 删除或重命名 `.env.local` 文件
2. 重启服务器
3. 开发模式自动启用

## ✅ 现在可以预览了！

刷新浏览器页面，访问 `http://localhost:3000/studio`，应该可以直接进入，无需登录！

页面顶部会显示开发模式横幅，表示正在使用模拟数据。
